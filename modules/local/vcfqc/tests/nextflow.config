process {
    executor = 'local'
    resourceLimits = [
        cpus: 2,
        memory: '2.GB',
        time: '30.m'
    ]
}

params {
    // Threshold for %N (sample passes if %N < percent_n)
    percent_n = 8
    enable_conda = false
}